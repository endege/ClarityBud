# Environment variables for ClarityBud

# Example for a database URL (if you were using a hosted DB like PostgreSQL)
# DATABASE_URL="postgresql://user:password@host:port/database"

# Other non-sensitive configurations can go here.
# API Keys or sensitive credentials should be managed securely,
# especially in production environments (e.g., via hosting provider's secret management).
# For local development, ensure this .env file is in .gitignore.